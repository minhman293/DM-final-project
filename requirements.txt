# Tested on Python 3.10 / 3.11, CUDA 12.x consumer GPU
# Install in a fresh venv:
#   pip install -r requirements.txt
#
# pytorch-forecasting pulls compatible pytorch-lightning; we pin the trio
# to known-good versions to avoid the recurring API breakages in this ecosystem.

torch>=2.1,<2.5
lightning>=2.1,<2.5
pytorch-lightning>=2.1,<2.5
pytorch-forecasting>=1.0.0,<1.3
pandas>=2.0
numpy>=1.24,<2.0
scikit-learn>=1.3
joblib>=1.3
tqdm>=4.66

# Optional but recommended — speeds up the cached weekly frame.
# If not installed, the pipeline silently falls back to pickle.
pyarrow>=14.0